<%= form_with(model: reclaim) do |form| %>
  <% if reclaim.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reclaim.errors.count, "error") %> prohibited this reclaim from being saved:</h2>

      <ul>
        <% reclaim.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <%= form.label :phone, style: "display: block" %>
    <%= form.text_field :phone %>
  </div>
 
  <div>
    <%= form.label :best_moment, style: "display: block" %>
    <%= form.text_field :best_moment %>
  </div>
 
  <div>
    <%= form.submit %>
  </div>
<% end %>
